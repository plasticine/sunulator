<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= title %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body class="bg-gray-200">
    <header class="container mx-auto mb-4">
      <div class="flex items-center py-4 mb-4">
        <%= link "☀️ Sunulator", to: "/", class: "text-2xl font-semibold" %>

        <nav class="flex-auto flex justify-end">
          <%= link "Locations", to: Routes.location_path(@conn, :index), class: "text-gray-600 bg-gray-300 hover:bg-gray-400 px-5 py-1 ml-4 rounded-full" %>
          <%= link "Scenarios", to: Routes.scenario_path(@conn, :index), class: "text-gray-600 bg-gray-300 hover:bg-gray-400 px-5 py-1 ml-4 rounded-full" %>
        </nav>
      </div>
    </header>

    <hr>

    <main role="main" class="container mx-auto">
      <%= if info = get_flash(@conn, :info) do %>
        <div class="mb-8 flex justify-center">
          <div class="p-2 px-6 bg-green-200 items-center text-green-700 leading-none lg:rounded-full flex lg:inline-flex" role="alert">
            <span class="font-medium flex-auto"><%= info %></span>
          </div>
        </div>
      <% end %>

      <%= if error = get_flash(@conn, :error) do %>
        <div class="mb-8 flex justify-center">
          <div class="p-2 px-6 bg-red-200 items-center text-red-700 leading-none lg:rounded-full flex lg:inline-flex" role="alert">
            <span class="font-medium px-4 text-left flex-auto"><%= error %></span>
          </div>
        </div>
      <% end %>

      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
